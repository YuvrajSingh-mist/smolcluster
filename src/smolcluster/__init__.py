"""SmolCluster — a distributed ML training and inference framework for small clusters of commodity hardware.

Supports data-parallelism (ClassicDP, SynchronousPS, EDP), model-parallelism (tensor + pipeline),
FSDP stages 0–2, expert parallelism (MoE), and GRPO fine-tuning with vLLM rollout workers.
"""
